# Copilot instructions

This repo's coding conventions, stack, testing strategy, and commit standards
are documented in `AGENTS.md` at the repo root (`CLAUDE.md` is a symlink to
it). Read it before making changes. Do not duplicate its rules here - if this
file and `AGENTS.md` disagree, `AGENTS.md` wins.
