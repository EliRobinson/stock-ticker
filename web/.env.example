# Copy this file to .env.local and fill in values.
# Never commit .env.local.
# All vars are validated in src/env.ts - the app will refuse to start/build
# if a required one is missing.
#
# This app has no vars of its own yet. It calls the Python API rather than
# a database directly - add NEXT_PUBLIC_API_URL here once that lands.
